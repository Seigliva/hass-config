alias: Slå av kaffetrakter etter to timer
initial_state: 'on'
trigger:
  - platform: state
    entity_id: switch.rf06
    to: 'on'
action:
  - delay:
      minutes: 1
  - service: switch.turn_off
    entity_id: switch.rf06
